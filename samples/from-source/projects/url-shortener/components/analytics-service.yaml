apiVersion: openchoreo.dev/v1alpha1
kind: Component
metadata:
  name: snip-analytics-service
  namespace: default
spec:
  owner:
    projectName: url-shortener

  componentType:
    kind: ClusterComponentType
    name: deployment/service

  autoDeploy: true

  workflow:
    kind: ClusterWorkflow
    name: dockerfile-builder
    parameters:
      repository:
        url: "https://github.com/openchoreo/sample-workloads"
        revision:
          branch: "main"
        appPath: "/project-url-shortener/analytics-service"
      docker:
        context: "/project-url-shortener/analytics-service"
        filePath: "/project-url-shortener/analytics-service/Dockerfile"

---
apiVersion: openchoreo.dev/v1alpha1
kind: WorkflowRun
metadata:
  name: snip-analytics-service-build-01
  labels:
    openchoreo.dev/project: "url-shortener"
    openchoreo.dev/component: "snip-analytics-service"
spec:
  workflow:
    kind: ClusterWorkflow
    name: dockerfile-builder
    parameters:
      repository:
        url: "https://github.com/openchoreo/sample-workloads"
        revision:
          branch: "main"
        appPath: "/project-url-shortener/analytics-service"
      docker:
        context: "/project-url-shortener/analytics-service"
        filePath: "/project-url-shortener/analytics-service/Dockerfile"
