apiVersion: openchoreo.dev/v1alpha1
kind: Component
metadata:
  name: snip-redis
  namespace: default
spec:
  owner:
    projectName: url-shortener

  componentType:
    kind: ClusterComponentType
    name: deployment/service

  autoDeploy: true

  workflow:
    kind: ClusterWorkflow
    name: dockerfile-builder
    parameters:
      repository:
        url: "https://github.com/openchoreo/sample-workloads"
        revision:
          branch: "main"
        appPath: "/project-url-shortener/redis"
      docker:
        context: "/project-url-shortener/redis"
        filePath: "/project-url-shortener/redis/Dockerfile"

---
apiVersion: openchoreo.dev/v1alpha1
kind: WorkflowRun
metadata:
  name: snip-redis-build-01
  labels:
    openchoreo.dev/project: "url-shortener"
    openchoreo.dev/component: "snip-redis"
spec:
  workflow:
    kind: ClusterWorkflow
    name: dockerfile-builder
    parameters:
      repository:
        url: "https://github.com/openchoreo/sample-workloads"
        revision:
          branch: "main"
        appPath: "/project-url-shortener/redis"
      docker:
        context: "/project-url-shortener/redis"
        filePath: "/project-url-shortener/redis/Dockerfile"
