# Build Plane values for k3d dev environment

# Disable external-secrets (already installed by data plane)
external-secrets:
  enabled: false

# Enable fake secret store for dev
fakeSecretStore:
  enabled: true

# Registry configuration
registry:
  persistence:
    enabled: true
    size: 5Gi
