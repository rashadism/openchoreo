# Data Plane values for k3d dev environment

# Disable cert-manager (already installed by control plane)
cert-manager:
  enabled: false

# Gateway configuration for k3d dev environment
gateway:
  # Enable gateway
  enabled: true

  # Use custom ports for k3d dev
  httpPort: 9080
  httpsPort: 9443
