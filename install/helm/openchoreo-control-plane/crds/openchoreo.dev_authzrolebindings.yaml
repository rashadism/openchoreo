apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: authzrolebindings.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: AuthzRoleBinding
    listKind: AuthzRoleBindingList
    plural: authzrolebindings
    singular: authzrolebinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AuthzRoleBinding is the Schema for the authzrolebindings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AuthzRoleBindingSpec defines the desired state of AuthzRoleBinding
            properties:
              effect:
                default: allow
                description: 'Effect indicates whether this binding allows or denies
                  access (default: allow)'
                enum:
                - allow
                - deny
                type: string
              entitlement:
                description: Entitlement defines the subject (from JWT claims) to
                  grant the role to
                properties:
                  claim:
                    description: Claim is the JWT claim name (e.g., "groups", "sub",
                      "email")
                    type: string
                  value:
                    description: Value is the entitlement value to match
                    type: string
                required:
                - claim
                - value
                type: object
              roleRef:
                description: |-
                  RoleRef references the role to bind
                  Can reference AuthzClusterRole (platform-wide) or AuthzRole (same namespace only)
                properties:
                  kind:
                    description: |-
                      Kind is the kind of role (AuthzRole or AuthzClusterRole)
                      For AuthzRoleBinding: AuthzRole must be in the same namespace
                    enum:
                    - AuthzRole
                    - AuthzClusterRole
                    type: string
                  name:
                    description: Name is the name of the role
                    type: string
                required:
                - kind
                - name
                type: object
              targetPath:
                description: |-
                  TargetPath defines which resources this binding applies to within the ownership hierarchy
                  All fields are optional - omitted fields mean "all" at that level
                properties:
                  component:
                    description: Component scopes to a specific component (optional)
                    type: string
                  project:
                    description: Project scopes to a specific project (optional)
                    type: string
                type: object
            required:
            - entitlement
            - roleRef
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
