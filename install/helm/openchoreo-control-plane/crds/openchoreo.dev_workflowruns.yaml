apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: workflowruns.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: WorkflowRun
    listKind: WorkflowRunList
    plural: workflowruns
    singular: workflowrun
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          WorkflowRun is the Schema for the workflowruns API
          WorkflowRun represents a runtime execution instance of a Workflow.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WorkflowRun
            properties:
              ttlAfterCompletion:
                description: |-
                  TTLAfterCompletion defines the time-to-live for this workflow run after completion.
                  This value is copied from the Workflow template.
                  Once the workflow completes, the run will be automatically deleted after this duration.
                  Format: duration string supporting days, hours, minutes, seconds without spaces (e.g., "90d", "10d1h30m", "1h30m")
                  Examples: "90d", "10d", "1h30m", "30m", "1d12h30m15s"
                pattern: ^(\d+d)?(\d+h)?(\d+m)?(\d+s)?$
                type: string
              workflow:
                description: Workflow configuration referencing the Workflow CR and
                  providing schema values.
                properties:
                  name:
                    description: |-
                      Name references the Workflow CR to use for this execution.
                      The Workflow CR contains the schema definition and resource template.
                    minLength: 1
                    type: string
                  parameters:
                    description: |-
                      Parameters contains the developer-provided values for the flexible parameter schema
                      defined in the referenced Workflow CR.

                      These values are validated against the Workflow's parameter schema.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - name
                type: object
            required:
            - workflow
            type: object
          status:
            description: status defines the observed state of WorkflowRun
            properties:
              completedAt:
                description: |-
                  CompletedAt is the timestamp when this workflow run finished execution (succeeded or failed).
                  This is used together with TTLAfterCompletion to determine when to delete the workflow run.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the WorkflowRun
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              resources:
                description: |-
                  Resources contains references to additional resources applied to the cluster.
                  These are tracked for cleanup when the WorkflowRun is deleted.
                items:
                  description: ResourceReference tracks a resource applied to the
                    build plane cluster for cleanup purposes.
                  properties:
                    apiVersion:
                      description: APIVersion is the API version of the resource (e.g.,
                        "v1", "apps/v1").
                      minLength: 1
                      type: string
                    kind:
                      description: Kind is the type of the resource (e.g., "Secret",
                        "ConfigMap").
                      minLength: 1
                      type: string
                    name:
                      description: Name is the name of the resource in the build plane
                        cluster.
                      minLength: 1
                      type: string
                    namespace:
                      description: |-
                        Namespace is the namespace of the resource in the build plane cluster.
                        Empty for cluster-scoped resources.
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
              runReference:
                description: |-
                  RunReference contains a reference to the workflow run resource that was applied to the cluster.
                  This tracks the actual workflow execution instance (e.g., Argo Workflow) in the target cluster.
                properties:
                  apiVersion:
                    description: APIVersion is the API version of the resource (e.g.,
                      "v1", "apps/v1").
                    minLength: 1
                    type: string
                  kind:
                    description: Kind is the type of the resource (e.g., "Secret",
                      "ConfigMap").
                    minLength: 1
                    type: string
                  name:
                    description: Name is the name of the resource in the build plane
                      cluster.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the resource in the build plane cluster.
                      Empty for cluster-scoped resources.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              startedAt:
                description: StartedAt is the timestamp when this workflow run started
                  execution.
                format: date-time
                type: string
              tasks:
                description: |-
                  Tasks contains the list of workflow tasks with their execution status.
                  This provides a vendor-neutral view of the workflow steps regardless of the underlying
                  workflow engine (e.g., Argo Workflows, Tekton).
                  Tasks are ordered by their execution sequence.
                items:
                  description: |-
                    WorkflowTask represents a single task/step in a workflow execution.
                    This provides a vendor-neutral abstraction over workflow engine-specific steps
                    (e.g., Argo Workflow nodes, Tekton TaskRuns).
                  properties:
                    completedAt:
                      description: CompletedAt is the timestamp when the task finished
                        execution.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        Message provides additional details about the task status.
                        This is typically populated when the task fails or errors.
                      type: string
                    name:
                      description: |-
                        Name is the name of the task/step.
                        For Argo Workflows, this corresponds to the templateName.
                      minLength: 1
                      type: string
                    phase:
                      description: Phase represents the current execution phase of
                        the task.
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Skipped
                      - Error
                      type: string
                    startedAt:
                      description: StartedAt is the timestamp when the task started
                        execution.
                      format: date-time
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
