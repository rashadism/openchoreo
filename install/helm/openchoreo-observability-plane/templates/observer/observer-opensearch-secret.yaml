apiVersion: v1
kind: Secret
metadata:
  name: observer-opensearch
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openchoreo-observability-plane.componentLabels" (dict "context" . "component" "observer") | nindent 4 }}
type: Opaque
data:
  username: {{ .Values.observer.openSearchUsername | b64enc }}
  password: {{ .Values.observer.openSearchPassword | b64enc }}
