{{- if not .Values.observer.openSearchSecretName }}
{{- fail "observer.openSearchSecretName is required. Create a Secret with 'username' and 'password' keys for OpenSearch authentication, then set observer.openSearchSecretName to its name." }}
{{- end }}
