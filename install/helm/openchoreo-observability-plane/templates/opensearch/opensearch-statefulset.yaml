{{- if eq .Values.global.installationMode "legacy" }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: opensearch
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openchoreo-observability-plane.componentLabels" (dict "context" . "component" "opensearch") | nindent 4 }}
spec:
  serviceName: opensearch
  replicas: {{ .Values.openSearchLegacy.replicas }}
  selector:
    matchLabels:
      {{- include "openchoreo-observability-plane.componentSelectorLabels" (dict "context" . "component" "opensearch") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openchoreo-observability-plane.componentSelectorLabels" (dict "context" . "component" "opensearch") | nindent 8 }}
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
      - name: opensearch
        image: "{{ .Values.openSearchLegacy.image.repository }}:{{ .Values.openSearchLegacy.image.tag }}"
        imagePullPolicy: {{ .Values.openSearchLegacy.image.pullPolicy }}
        env:
        - name: cluster.name
          value: {{ .Values.openSearchLegacy.config.clusterName }}
        - name: node.name
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: discovery.type
          value: {{ .Values.openSearchLegacy.config.discoveryType }}
        - name: OPENSEARCH_JAVA_OPTS
          value: {{ .Values.openSearchLegacy.config.javaOpts | quote }}
        - name: bootstrap.memory_lock
          value: {{ .Values.openSearchLegacy.config.memoryLock | quote }}
        - name: DISABLE_SECURITY_PLUGIN
          value: {{ .Values.openSearchLegacy.config.disableSecurity | quote }}
        ports:
        - containerPort: {{ .Values.openSearchLegacy.service.httpPort }}
          name: http
        - containerPort: {{ .Values.openSearchLegacy.service.transportPort }}
          name: transport
        volumeMounts:
        - name: data
          mountPath: /usr/share/opensearch/data
        resources:
          {{- toYaml .Values.openSearchLegacy.resources | nindent 10 }}
  {{- if .Values.openSearchLegacy.persistence.enabled }}
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes:
      - {{ .Values.openSearchLegacy.persistence.accessMode }}
      {{- if .Values.openSearchLegacy.persistence.storageClass }}
      storageClassName: {{ .Values.openSearchLegacy.persistence.storageClass }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.openSearchLegacy.persistence.size }}
  {{- end }}
{{- end }}
