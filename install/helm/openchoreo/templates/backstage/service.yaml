apiVersion: v1
kind: Service
metadata:
  name: {{ include "openchoreo.fullname" . }}-ui
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openchoreo.labels" . | nindent 4 }}
    app.kubernetes.io/component: backstage
spec:
  type: {{ .Values.backstage.service.type }}
  ports:
  - port: {{ .Values.backstage.service.port }}
    targetPort: http
    protocol: TCP
    name: http
    {{- if and (eq .Values.backstage.service.type "NodePort") .Values.backstage.service.nodePort }}
    nodePort: {{ .Values.backstage.service.nodePort }}
    {{- end }}
  selector:
    app.kubernetes.io/component: backstage
    {{- include "openchoreo.selectorLabels" . | nindent 4 }}
