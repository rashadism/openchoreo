apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: componenttypedefinitions.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: ComponentTypeDefinition
    listKind: ComponentTypeDefinitionList
    plural: componenttypedefinitions
    shortNames:
    - ctd
    - ctds
    singular: componenttypedefinition
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.workloadType
      name: WorkloadType
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ComponentTypeDefinition is the Schema for the componenttypedefinitions
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComponentTypeDefinitionSpec defines the desired state of
              ComponentTypeDefinition.
            properties:
              resources:
                description: |-
                  Resources are templates that generate Kubernetes resources dynamically
                  At least one resource must be defined with an id matching the workloadType
                items:
                  description: ResourceTemplate defines a template for generating
                    Kubernetes resources
                  properties:
                    forEach:
                      description: |-
                        ForEach enables generating multiple resources from a list using CEL expression
                        Example: "${spec.configurations}" to iterate over a list
                      pattern: ^\$\{.+\}$
                      type: string
                    id:
                      description: |-
                        ID uniquely identifies this resource within the component type
                        For the primary workload resource, this must match the workloadType
                      minLength: 1
                      type: string
                    includeWhen:
                      description: |-
                        IncludeWhen is a CEL expression that determines if this resource should be created
                        If not specified, the resource is always created
                        Example: "${spec.autoscaling.enabled}"
                      type: string
                    template:
                      description: |-
                        Template contains the Kubernetes resource with CEL expressions
                        CEL expressions are enclosed in ${...} and will be evaluated at runtime
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    var:
                      description: |-
                        Var is the loop variable name when using forEach
                        Example: "config" will make each item available as ${config} in templates
                      pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                      type: string
                  required:
                  - id
                  - template
                  type: object
                  x-kubernetes-validations:
                  - message: var is required when forEach is specified
                    rule: '!has(self.forEach) || has(self.var)'
                minItems: 1
                type: array
              schema:
                description: Schema defines what developers can configure when creating
                  components of this type
                properties:
                  envOverrides:
                    description: |-
                      EnvOverrides can be overridden per environment via EnvSettings by platform engineers.
                      These are also exposed to developers but can be changed per environment.
                      Same nested map structure and type definition format as Parameters.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  parameters:
                    description: |-
                      Parameters are static across environments and exposed as inputs to developers
                      when creating a Component of this type. This is a nested map structure where
                      keys are field names and values are either nested maps or type definition strings.
                      Type definition format: "type | default=value | required=true | enum=val1,val2"
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              workloadType:
                description: |-
                  WorkloadType must be one of: deployment, statefulset, cronjob, job
                  This determines the primary workload resource type for this component type
                enum:
                - deployment
                - statefulset
                - cronjob
                - job
                type: string
                x-kubernetes-validations:
                - message: spec.workloadType cannot be changed after creation
                  rule: self == oldSelf
            required:
            - resources
            - workloadType
            type: object
            x-kubernetes-validations:
            - message: resources must contain a primary resource with id matching
                workloadType
              rule: self.resources.exists(r, r.id == self.workloadType)
          status:
            description: ComponentTypeDefinitionStatus defines the observed state
              of ComponentTypeDefinition.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the ComponentTypeDefinition's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed ComponentTypeDefinition
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
