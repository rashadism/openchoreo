## Traces ({{ total }} total)

{% if total > 50 -%}
> **Warning**: {{ total }} traces returned. Narrow your time window closer to the alert timestamp to reduce noise.
{% endif -%}

Trace ID | Name | Spans | Duration | Errors | Start Time
--- | --- | --- | --- | --- | ---
{% for trace in traces -%}
{{ trace.traceId }} | {{ trace.traceName }} | {{ trace.spanCount }} | {{ "%.2f"|format(trace.durationNs / 1000000) }}ms | {{ "yes" if trace.hasErrors else "no" }} | {{ trace.startTime }}
{% endfor -%}

> Call `query_trace_spans` with a traceId above to inspect the full span tree for latency bottlenecks, errors, or failing dependencies. Prefer traces whose Errors column is `yes` — a slow trace that succeeded is rarely the cause of a failure alert.
