## Traces ({{ .Total }} total)
{{ if gt .Total 50 }}
> **Warning**: {{ .Total }} traces returned. Narrow your time window closer to the alert timestamp to reduce noise.
{{ end }}
| Trace ID | Name | Spans | Duration | Start Time |
|----------|------|-------|----------|------------|
{{ range .Traces -}}
| {{ .TraceID }} | {{ escapeMD .TraceName }} | {{ .SpanCount }} | {{ printf "%.2f" .DurationMs }}ms | {{ .StartTime }} |
{{ end }}
> Call `query_trace_spans` with a traceId above to inspect the full span tree for latency bottlenecks, errors, or failing dependencies.
