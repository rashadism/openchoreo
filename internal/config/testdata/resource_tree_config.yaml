resource_tree:
  rules:
    - root:
        group: apps
        version: v1
        kind: Deployment
        resource: deployments
      children:
        - kind:
            group: apps
            version: v1
            kind: ReplicaSet
            resource: replicasets
          hide: true
