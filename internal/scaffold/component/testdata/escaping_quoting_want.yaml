# Generated by occ scaffold component
# Component: my-api
# Type: deployment/web-app
apiVersion: openchoreo.dev/v1alpha1
kind: Component
metadata:
  name: my-api
  namespace: acme-corp
spec:
  owner:
    projectName: online-store
  componentType:
    kind: ComponentType
    name: deployment/web-app
  # autoDeploy: true # Enable automatic deployment on changes

  # Parameters for the ComponentType
  parameters:
    environment: development environment # Enum with spaces in values | also: staging environment, production environment
    logFormat: json # Enum with special characters | also: text, key=value pairs

    # Defaults: Uncomment to customize
    # greeting: Hello, World! # Default with comma and exclamation
    # multiline: line1\nline2 # Default with escaped newline
    # queryTemplate: SELECT * FROM "users" WHERE status = 'active' # Default with quotes inside
    # timeFormat: HH:mm:ss # Default with colons (YAML special)
    # trueFalseString: true # Boolean-like string that needs quoting
    # zipCode: 00123 # Number-like string with leading zeros
    # specialLabels: # Map with special key format
      # app.kubernetes.io/name: my-app
      # version: 1.0.0
    # specialTags: # Array with special string values
      # - tag:value
      # - key=pair
      # - with spaces
