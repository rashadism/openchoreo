# objectRef is a planned matcher that this binary does not implement yet.
resource_tree:
  rules:
    - root:
        group: apps
        version: v1
        kind: Deployment
        resource: deployments
      children:
        - kind:
            version: v1
            kind: Pod
            resource: pods
          matcher: objectRef
