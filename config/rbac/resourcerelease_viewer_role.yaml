# permissions for end users to view resourcereleases.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: openchoreo
    app.kubernetes.io/managed-by: kustomize
  name: resourcerelease-viewer-role
rules:
- apiGroups:
  - openchoreo.dev
  resources:
  - resourcereleases
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - openchoreo.dev
  resources:
  - resourcereleases/status
  verbs:
  - get
