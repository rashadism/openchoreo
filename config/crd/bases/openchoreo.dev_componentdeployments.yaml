---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: componentdeployments.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: ComponentDeployment
    listKind: ComponentDeploymentList
    plural: componentdeployments
    shortNames:
    - compdeployment
    - compdeployments
    singular: componentdeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.owner.componentName
      name: Component
      type: string
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ComponentDeployment is the Schema for the componentdeployments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComponentDeploymentSpec defines the desired state of ComponentDeployment.
            properties:
              addonOverrides:
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  AddonOverrides provides environment-specific overrides for addon configurations
                  Keyed by instanceName (which must be unique across all addons in the component)
                  Structure: map[instanceName]overrideValues
                type: object
              configurationOverrides:
                description: |-
                  ConfigurationOverrides provides environment-specific overrides for workload configurations
                  These values override or add to the configurations defined in the workload.yaml
                properties:
                  env:
                    description: Environment variable overrides
                    items:
                      description: EnvVar represents an environment variable present
                        in the container.
                      properties:
                        key:
                          description: The environment variable key.
                          type: string
                        value:
                          description: |-
                            The literal value of the environment variable.
                            Mutually exclusive with valueFrom.
                          type: string
                        valueFrom:
                          description: |-
                            Extract the environment variable value from another resource.
                            Mutually exclusive with value.
                          properties:
                            configurationGroupRef:
                              description: Reference to a configuration group.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                            secretRef:
                              description: Reference to a secret resource.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - key
                      type: object
                    type: array
                  file:
                    description: File configuration overrides
                    items:
                      description: |-
                        FileVar represents a file configuration in a container.
                        FileVar represents a file configuration in a container.
                      properties:
                        key:
                          description: The file key/name.
                          type: string
                        mountPath:
                          description: The mount path where the file will be mounted.
                          type: string
                        value:
                          description: |-
                            The literal content of the file.
                            Mutually exclusive with secretKeyRef.
                          type: string
                        valueFrom:
                          description: |-
                            Extract the environment variable value from another resource.
                            Mutually exclusive with value.
                          properties:
                            configurationGroupRef:
                              description: Reference to a configuration group.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                            secretRef:
                              description: Reference to a secret resource.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - key
                      - mountPath
                      type: object
                    type: array
                type: object
              environment:
                description: Environment is the name of the environment this deployment
                  applies to
                minLength: 1
                type: string
              overrides:
                description: |-
                  Overrides for ComponentTypeDefinition envOverrides parameters
                  These values override the defaults defined in the Component for this specific environment
                type: object
                x-kubernetes-preserve-unknown-fields: true
              owner:
                description: Owner identifies the component this deployment applies
                  to
                properties:
                  componentName:
                    description: ComponentName is the name of the component this deployment
                      applies to
                    minLength: 1
                    type: string
                  projectName:
                    description: ProjectName is the name of the project that owns
                      this component
                    minLength: 1
                    type: string
                required:
                - componentName
                - projectName
                type: object
            required:
            - environment
            - owner
            type: object
          status:
            description: ComponentDeploymentStatus defines the observed state of ComponentDeployment.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the ComponentDeployment's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed ComponentDeployment
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
