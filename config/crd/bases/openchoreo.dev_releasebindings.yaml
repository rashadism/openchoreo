---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: releasebindings.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: ReleaseBinding
    listKind: ReleaseBindingList
    plural: releasebindings
    singular: releasebinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.owner.projectName
      name: Project
      type: string
    - jsonPath: .spec.owner.componentName
      name: Component
      type: string
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .status.invokeURL
      name: InvokeURL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ReleaseBinding is the Schema for the releasebindings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReleaseBindingSpec defines the desired state of ReleaseBinding.
            properties:
              componentTypeEnvOverrides:
                description: |-
                  ComponentTypeEnvOverrides for ComponentType envOverrides parameters
                  These values override the defaults defined in the Component for this specific environment
                type: object
                x-kubernetes-preserve-unknown-fields: true
              environment:
                description: EnvironmentName is the name of the environment this binds
                  the ComponentRelease to
                type: string
                x-kubernetes-validations:
                - message: spec.environment is immutable
                  rule: self == oldSelf
              owner:
                description: Owner identifies the component and project this ReleaseBinding
                  belongs to
                properties:
                  componentName:
                    description: ComponentName is the name of the component
                    minLength: 1
                    type: string
                  projectName:
                    description: ProjectName is the name of the project that owns
                      this component
                    minLength: 1
                    type: string
                required:
                - componentName
                - projectName
                type: object
                x-kubernetes-validations:
                - message: spec.owner is immutable
                  rule: self == oldSelf
              releaseName:
                description: |-
                  ReleaseName is the name of the ComponentRelease to bind
                  When ComponentSpec.AutoDeploy is enabled, this field will be handled by the controller
                type: string
              state:
                default: Active
                description: |-
                  State controls the state of the Release created by this binding.
                  Active: Resources are deployed normally
                  Undeploy: Resources are removed from the data plane
                enum:
                - Active
                - Undeploy
                type: string
              traitOverrides:
                additionalProperties:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  TraitOverrides provides environment-specific overrides for trait configurations
                  Keyed by instanceName (which must be unique across all traits in the component)
                  Structure: map[instanceName]overrideValues
                type: object
              workloadOverrides:
                description: |-
                  WorkloadOverrides provides environment-specific overrides for the entire workload spec
                  These values override the workload specification for this specific environment
                properties:
                  containers:
                    additionalProperties:
                      description: ContainerOverride represents a single container
                        in the workload.
                      properties:
                        env:
                          description: Explicit environment variables.
                          items:
                            description: EnvVar represents an environment variable
                              present in the container.
                            properties:
                              key:
                                description: The environment variable key.
                                type: string
                              value:
                                description: |-
                                  The literal value of the environment variable.
                                  Mutually exclusive with valueFrom.
                                type: string
                              valueFrom:
                                description: |-
                                  Extract the environment variable value from another resource.
                                  Mutually exclusive with value.
                                properties:
                                  configurationGroupRef:
                                    description: Reference to a configuration group.
                                    properties:
                                      key:
                                        type: string
                                      name:
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  secretRef:
                                    description: Reference to a secret resource.
                                    properties:
                                      key:
                                        type: string
                                      name:
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                type: object
                            required:
                            - key
                            type: object
                          type: array
                        files:
                          description: File configurations.
                          items:
                            description: FileVar represents a file configuration in
                              a container.
                            properties:
                              key:
                                description: The file key/name.
                                type: string
                              mountPath:
                                description: The mount path where the file will be
                                  mounted.
                                type: string
                              value:
                                description: |-
                                  The literal content of the file.
                                  Mutually exclusive with valueFrom.
                                type: string
                              valueFrom:
                                description: |-
                                  Extract the environment variable value from another resource.
                                  Mutually exclusive with value.
                                properties:
                                  configurationGroupRef:
                                    description: Reference to a configuration group.
                                    properties:
                                      key:
                                        type: string
                                      name:
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  secretRef:
                                    description: Reference to a secret resource.
                                    properties:
                                      key:
                                        type: string
                                      name:
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                type: object
                            required:
                            - key
                            - mountPath
                            type: object
                            x-kubernetes-validations:
                            - message: exactly one of value or valueFrom must be set
                              rule: has(self.value) != has(self.valueFrom)
                          type: array
                      type: object
                    description: |-
                      Containers define the container specifications for this workload.
                      The key is the container name, and the value is the container specification.
                    type: object
                type: object
                x-kubernetes-preserve-unknown-fields: true
            required:
            - environment
            - owner
            type: object
          status:
            description: ReleaseBindingStatus defines the observed state of ReleaseBinding.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the ReleaseBinding's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              invokeURL:
                description: |-
                  InvokeURL is the resolved public URL derived from the HTTPRoute rendered for this binding.
                  It is populated once the component is deployed and an HTTPRoute with a public hostname is available.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
