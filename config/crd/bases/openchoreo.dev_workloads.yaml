---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: workloads.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: Workload
    listKind: WorkloadList
    plural: workloads
    singular: workload
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.owner.projectName
      name: Project
      type: string
    - jsonPath: .spec.owner.componentName
      name: Component
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workload is the Schema for the workloads API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              connections:
                additionalProperties:
                  description: WorkloadConnection represents an internal API connection
                  properties:
                    inject:
                      description: Inject defines how connection details are injected
                        into the workload
                      properties:
                        env:
                          description: Environment variables to inject
                          items:
                            description: WorkloadConnectionEnvVar defines an environment
                              variable injection
                            properties:
                              name:
                                description: Environment variable name
                                type: string
                              value:
                                description: Template value using connection properties
                                  (e.g., "{{ .url }}")
                                type: string
                            required:
                            - name
                            - value
                            type: object
                          type: array
                      required:
                      - env
                      type: object
                    params:
                      additionalProperties:
                        type: string
                      description: Parameters for connection configuration (dynamic
                        key-value pairs)
                      type: object
                    type:
                      description: Type of connection - only "api" for now
                      enum:
                      - api
                      type: string
                  required:
                  - inject
                  - type
                  type: object
                description: |-
                  Connections define how this workload consumes internal and external resources.
                  The key is the connection name, and the value is the connection specification.
                type: object
              container:
                description: Container defines the container specification for this
                  workload.
                properties:
                  args:
                    items:
                      type: string
                    type: array
                  command:
                    description: Container entrypoint & args.
                    items:
                      type: string
                    type: array
                  env:
                    description: Explicit environment variables.
                    items:
                      description: EnvVar represents an environment variable present
                        in the container.
                      properties:
                        key:
                          description: The environment variable key.
                          type: string
                        value:
                          description: |-
                            The literal value of the environment variable.
                            Mutually exclusive with valueFrom.
                          type: string
                        valueFrom:
                          description: |-
                            Extract the environment variable value from another resource.
                            Mutually exclusive with value.
                          properties:
                            configurationGroupRef:
                              description: Reference to a configuration group.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                            secretRef:
                              description: Reference to a secret resource.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - key
                      type: object
                    type: array
                  files:
                    description: File configurations.
                    items:
                      description: FileVar represents a file configuration in a container.
                      properties:
                        key:
                          description: The file key/name.
                          type: string
                        mountPath:
                          description: The mount path where the file will be mounted.
                          type: string
                        value:
                          description: |-
                            The literal content of the file.
                            Mutually exclusive with valueFrom.
                          type: string
                        valueFrom:
                          description: |-
                            Extract the environment variable value from another resource.
                            Mutually exclusive with value.
                          properties:
                            configurationGroupRef:
                              description: Reference to a configuration group.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                            secretRef:
                              description: Reference to a secret resource.
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - key
                      - mountPath
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of value or valueFrom must be set
                        rule: has(self.value) != has(self.valueFrom)
                    type: array
                  image:
                    description: OCI image to run (digest or tag).
                    minLength: 1
                    type: string
                required:
                - image
                type: object
              endpoints:
                additionalProperties:
                  description: WorkloadEndpoint represents a simple network endpoint
                    for basic exposure.
                  properties:
                    basePath:
                      description: BasePath is the base path of the API exposed via
                        the endpoint.
                      type: string
                    displayName:
                      description: DisplayName is an optional human-readable name
                        for the endpoint.
                      type: string
                    port:
                      description: Port exposed by the endpoint. If targetPort is
                        not set, platform defaults to port for both.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    schema:
                      description: Schema for the endpoint API definition.
                      properties:
                        content:
                          type: string
                        type:
                          type: string
                      type: object
                    targetPort:
                      description: TargetPort maps to the container listening port.
                        Optional — defaults to port.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    type:
                      description: Type indicates the protocol/technology of the endpoint.
                      enum:
                      - HTTP
                      - REST
                      - gRPC
                      - GraphQL
                      - Websocket
                      - TCP
                      - UDP
                      type: string
                    visibility:
                      description: |-
                        Visibility is an array of additional endpoint visibilities beyond the implicit project visibility.
                        Every endpoint always gets project visibility. This array adds extra scopes.
                      items:
                        description: |-
                          EndpointVisibility defines the visibility scope for an endpoint.
                          It determines which components can access the endpoint and how that access is enforced at runtime.
                        enum:
                        - project
                        - namespace
                        - internal
                        - external
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                  required:
                  - port
                  - type
                  type: object
                description: |-
                  Endpoints define simple network endpoints for basic port exposure.
                  The key is the endpoint name, and the value is the endpoint specification.
                type: object
              owner:
                properties:
                  componentName:
                    minLength: 1
                    type: string
                  projectName:
                    minLength: 1
                    type: string
                required:
                - componentName
                - projectName
                type: object
            required:
            - container
            - owner
            type: object
          status:
            description: WorkloadStatus defines the observed state of Workload.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
