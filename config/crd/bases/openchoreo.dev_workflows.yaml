---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: workflows.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Workflow is the Schema for the workflows API
          Workflow represents a runtime execution instance of a WorkflowDefinition.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Workflow
            properties:
              owner:
                description: |-
                  Owner identifies the Component that owns this Workflow.
                  This is used for tracking and reporting purposes.
                properties:
                  componentName:
                    description: ComponentName is the name of the owning Component
                    minLength: 1
                    type: string
                  projectName:
                    description: ProjectName is the name of the owning Project
                    minLength: 1
                    type: string
                required:
                - componentName
                - projectName
                type: object
              schema:
                description: |-
                  Parameters contains the developer-provided values that conform to the schema
                  defined in the referenced WorkflowDefinition.

                  These parameters are merged with fixed parameters and context variables
                  when rendering the final workflow resource.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              workflowDefinitionRef:
                description: |-
                  WorkflowDefinitionRef references the WorkflowDefinition to use for this execution.
                  The WorkflowDefinition contains the schema, fixed parameters, and resource template.
                type: string
            required:
            - owner
            - workflowDefinitionRef
            type: object
          status:
            description: status defines the observed state of Workflow
            properties:
              completionTime:
                description: CompletionTime is the time when the workflow completed
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the Workflow
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message provides a human-readable message about the workflow
                  status
                type: string
              phase:
                description: |-
                  Phase represents the current phase of the workflow execution.
                  Possible values: Pending, Running, Succeeded, Failed, Error
                type: string
              startTime:
                description: StartTime is the time when the workflow started execution
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
