---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: clusterauthzrolebindings.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: ClusterAuthzRoleBinding
    listKind: ClusterAuthzRoleBindingList
    plural: clusterauthzrolebindings
    singular: clusterauthzrolebinding
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterAuthzRoleBinding is the Schema for the clusterauthzrolebindings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterAuthzRoleBindingSpec defines the desired state of
              ClusterAuthzRoleBinding
            properties:
              effect:
                default: allow
                description: 'Effect indicates whether this binding allows or denies
                  access (default: allow)'
                enum:
                - allow
                - deny
                type: string
              entitlement:
                description: Entitlement defines the subject (from JWT claims) to
                  grant the role to
                properties:
                  claim:
                    description: Claim is the JWT claim name (e.g., "groups", "sub",
                      "email")
                    type: string
                  value:
                    description: Value is the entitlement value to match
                    type: string
                required:
                - claim
                - value
                type: object
              roleMappings:
                description: RoleMappings is the list of cluster roles this binding
                  grants
                items:
                  description: ClusterRoleMapping pairs a role reference with an optional
                    scope for cluster-scoped bindings
                  properties:
                    conditions:
                      description: |-
                        Conditions define attribute-based restrictions on specific actions granted by the role.
                        Multiple entries whose actions match the request are combined with OR semantics —
                        at least one matching entry must pass for the action to be permitted.
                        If omitted, no attribute restrictions apply — the RBAC grant stands as-is.
                      items:
                        description: AuthzCondition represents the conditions under
                          which an action is allowed or denied in an authorization
                          role.
                        properties:
                          actions:
                            description: |-
                              Actions is the list of actions this condition applies to.
                              Supports exact match ("releasebinding:create") and wildcards ("releasebinding:*").
                            items:
                              type: string
                            minItems: 1
                            type: array
                          expression:
                            description: |-
                              Expression is a CEL expression that must evaluate to true for the action to be permitted.
                              Examples:
                                resource.environment in ["dev", "staging"]
                            minLength: 1
                            type: string
                        required:
                        - actions
                        - expression
                        type: object
                      type: array
                    roleRef:
                      description: RoleRef references the ClusterAuthzRole to bind
                      properties:
                        kind:
                          description: |-
                            Kind is the kind of role (AuthzRole or ClusterAuthzRole)
                            For AuthzRoleBinding: AuthzRole must be in the same namespace
                          enum:
                          - AuthzRole
                          - ClusterAuthzRole
                          type: string
                        name:
                          description: Name is the name of the role
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    scope:
                      description: Scope defines the target scope within the ownership
                        hierarchy
                      properties:
                        component:
                          description: Component scopes to a specific component (optional,
                            requires project)
                          type: string
                        namespace:
                          description: Namespace scopes to a specific namespace (optional)
                          type: string
                        project:
                          description: Project scopes to a specific project (optional,
                            requires namespace)
                          type: string
                        resource:
                          description: Resource scopes to a specific resource (optional,
                            requires project)
                          type: string
                      type: object
                  required:
                  - roleRef
                  type: object
                  x-kubernetes-validations:
                  - message: scope.project requires scope.namespace, and scope.component
                      requires scope.project
                    rule: '!has(self.scope) || (!has(self.scope.project) || has(self.scope.namespace))
                      && (!has(self.scope.component) || has(self.scope.project))'
                  - message: scope.resource requires scope.project
                    rule: '!has(self.scope) || !has(self.scope.resource) || has(self.scope.project)'
                  - message: scope.component and scope.resource are mutually exclusive
                    rule: '!has(self.scope) || !has(self.scope.component) || !has(self.scope.resource)'
                minItems: 1
                type: array
            required:
            - entitlement
            - roleMappings
            type: object
            x-kubernetes-validations:
            - message: ClusterAuthzRoleBinding can only reference ClusterAuthzRole
              rule: self.roleMappings.all(m, m.roleRef.kind == 'ClusterAuthzRole')
        type: object
    served: true
    storage: true
    subresources: {}
