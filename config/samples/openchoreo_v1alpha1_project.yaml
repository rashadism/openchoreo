apiVersion: openchoreo.dev/v1alpha1
kind: Project
metadata:
  name: internal-apps
  namespace: default
  annotations:
    openchoreo.dev/display-name: Internal Applications
    openchoreo.dev/description: This project contains components that are used by companies internal applications
  labels:
    openchoreo.dev/name: internal-apps
spec:
  deploymentPipelineRef: default-deployment-pipeline
  # Optional: Specify a BuildPlane or ClusterBuildPlane for build operations
  # If not specified, defaults to "default" BuildPlane in the same namespace
  # buildPlaneRef:
  #   kind: BuildPlane  # or ClusterBuildPlane
  #   name: default
